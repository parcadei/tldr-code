{
  "task": "Consolidate tldr's filesystem walking and directory-level language detection into single implementations. After consolidation, re-assess the two remaining structural bugs (#2 change-impact empty-success, #6 impact 0-callers-with-AST-hit) against the cleaned codebase. The 6 bugs in GitHub issue parcadei/tldr-code#1 stem from two root causes: (a) ~30 raw `walkdir::WalkDir` call sites that don't honor .gitignore or skip vendor dirs, and (b) two competing directory-level language detectors where the broken one (Language::from_directory) is used by 15 commands and the filtered one (surface::detect_lang_from_directory) is used by only 1.",
  "complexity": "multi-feature",
  "repro_fixture": "/tmp/tldr-real/dub (dubinc/dub shallow clone, pnpm install --ignore-scripts complete, 3928 .ts/.tsx, 2877 nested node_modules dirs, 2.4GB)",
  "milestones": [
    {
      "name": "walker-consolidation",
      "status": "passed",
      "assertions": ["VAL-001"]
    },
    {
      "name": "detector-consolidation",
      "status": "passed",
      "assertions": ["VAL-002"]
    },
    {
      "name": "reassess-structural-bugs",
      "status": "passed",
      "assertions": ["VAL-003"]
    },
    {
      "name": "parser-dialect",
      "status": "pending",
      "assertions": ["VAL-004"]
    },
    {
      "name": "change-impact-status",
      "status": "pending",
      "assertions": ["VAL-005"]
    },
    {
      "name": "vuln-autodetect-and-cap",
      "status": "pending",
      "assertions": ["VAL-006"]
    },
    {
      "name": "workspace-discovery",
      "status": "pending",
      "assertions": ["VAL-007"]
    }
  ],
  "assertions": [
    {
      "id": "VAL-001",
      "type": "invariant",
      "status_resolution": "PASSED 2026-04-24 commit dc896a7 — walker module + 30 migrations + 4 integration tests + --no-default-ignore on 4 cmds + vuln/secure --lang gating. See validation/m1-walker-consolidation.json. NOTE: smells still hangs on dub root due to SEPARATE pre-existing analyzer bug (hangs on single-file screenshot.tsx, bypasses walker entirely) — out of scope for VAL-001, documented for M3 reassessment.",
      "text": "All command and analysis code that walks a project directory uses a single shared walker built on `ignore::WalkBuilder` that (a) honors `.gitignore`, (b) skips hidden dirs, (c) skips a default-exclude list of `node_modules`, `target`, `dist`, `build`, `.next`, `__pycache__`, `vendor`, `.git`, (d) sets `follow_links(false)` to avoid pnpm symlink loops. The module lives at `crates/tldr-core/src/walker.rs` (new) exposing `pub fn walk_project(root: &Path) -> impl Iterator<Item = ignore::DirEntry>` plus a builder for opt-outs (`no_default_ignore`, `max_depth`, custom extension filter). Every existing `walkdir::WalkDir::new(path)` call in these files migrates to the new walker: `crates/tldr-cli/src/commands/{dead.rs,patterns/cohesion.rs,patterns/interface.rs,patterns/temporal.rs,contracts/invariants.rs,contracts/specs.rs,contracts/verify.rs,remaining/vuln.rs,remaining/secure.rs,remaining/api_check.rs,remaining/todo.rs,daemon/warm.rs}`, `crates/tldr-core/src/{wrappers/todo.rs,analysis/references.rs,analysis/clones/filter.rs,quality/cohesion.rs,quality/complexity.rs,quality/coupling.rs,quality/health.rs,quality/dead_code.rs,quality/maintainability.rs,quality/martin.rs,quality/similarity.rs,quality/smells.rs,types.rs}`. EXCEPTIONS (stay on raw walkdir): `crates/tldr-core/examples/*.rs` (benchmarks), `crates/tldr-cli/src/commands/archived/*` (archived), and any `fs::read_dir` in `core/surface/*.rs` that scans a single package directory looking for a manifest — those are intentionally non-recursive and non-project-wide. Acceptance: (1) `tldr smells /tmp/tldr-real/dub` completes in <60s with zero findings whose `file` contains `/node_modules/`. (2) `tldr secure /tmp/tldr-real/dub` produces zero findings under `/node_modules/`. (3) `tldr vuln /tmp/tldr-real/dub --lang typescript --format json --quiet` produces zero findings under `/node_modules/` AND skips .py files entirely (check: `is_supported_source_file` in vuln.rs now language-aware via `--lang`). (4) `cargo test --workspace` passes. (5) `cargo clippy --workspace -- -D warnings` passes. (6) `grep -rn 'WalkDir::new' crates/tldr-cli/src/commands crates/tldr-core/src/quality crates/tldr-core/src/analysis/references.rs crates/tldr-core/src/wrappers/todo.rs` returns only occurrences inside the new `walker.rs` module itself or explicit non-project walks. Add an opt-out flag `--no-default-ignore` to smells/secure/vuln/dead (top 4 commands users might want to override — pattern after `loc.rs`'s `no_gitignore: bool` at line 71-73).",
      "milestone": "walker-consolidation",
      "status": "passed",
      "depends": [],
      "worker": "kraken (agent af5070fe8fa6f8b34)",
      "evidence": "reports/m1-walker-migration.json, validation/m1-walker-consolidation.json, commit dc896a7"
    },
    {
      "id": "VAL-002",
      "type": "invariant",
      "status_resolution": "PASSED 2026-04-24 — Language::from_directory rewritten with manifest priority (tsconfig.json > package.json > Cargo.toml > go.mod > pyproject.toml/setup.py/requirements.txt > pom.xml > build.gradle.kts/build.gradle > Gemfile > composer.json > mix.exs > Package.swift) scanning depth<=2 (covers pnpm packages/*/ and apps/*/ layouts). Precedence-beats-depth so tsconfig.json at depth 2 wins over root package.json without TS dep — returns TypeScript for dub. Extension-majority fallback via walk_project when no manifest found. Deleted surface::detect_lang_from_directory/detect_lang_from_directory_recursive (58 lines) + the three fn detect_language(path) in quality/{dead_code,health,martin}.rs. Single caller at surface::detect_lang_from_path delegates to Language::from_directory via as_str(). Added 20 tests (pnpm monorepo, manifests, node_modules-skip, fallback). tldr-core lib: 4616 passed/0 failed/340 ignored (+20 tests). See reports/m2-detector-consolidation.json.",
      "text": "There is exactly one directory-level language detector in the codebase. Rewrite `Language::from_directory` at `crates/tldr-core/src/types.rs:150` so that it (a) uses the shared walker from VAL-001 (which auto-excludes node_modules), (b) first consults manifest files as strong signals: `tsconfig.json` → TypeScript, `package.json` with `\"typescript\"` in deps → TypeScript else JavaScript, `Cargo.toml` → Rust, `go.mod` → Go, `pyproject.toml` or `setup.py` → Python, `pom.xml` or `build.gradle(.kts)` → Java, (c) falls back to extension-majority via the new walker when no manifest matches. Delete `fn detect_lang_from_directory` and `fn detect_lang_from_directory_recursive` at `crates/tldr-core/src/surface/mod.rs:229-272` and update its single caller at `surface/mod.rs:179` to use `Language::from_directory` (convert between `&'static str` and `Language` enum as needed, or change that caller to return `Language` directly). Keep `Language::from_path` (file-extension, widely used, correct). Keep `surface::detect_lang_from_path` for the single-target string case at `surface/mod.rs:174`. Keep the file-extension wrappers that just alias `Language::from_path` (`dice.rs:192`, `patterns/coupling.rs:306`). DELETE the three copy-pasted `fn detect_language(path: &Path)` in `crates/tldr-core/src/quality/{dead_code.rs:512, health.rs:880, martin.rs:700}` — replace with `Language::from_path` calls. DO NOT touch `api_check.rs::detect_language` (parallel ApiLanguage enum, separate concern) or `fix/error_parser.rs::detect_language` (error-text parsing, different purpose). Update the test `test_language_from_directory_detects_majority` at `types.rs:1923` to reflect new manifest-priority behavior, and add tests for: (a) TS project with node_modules containing Python stubs → detects TypeScript, (b) pure Python project → detects Python, (c) Rust project → detects Rust. Acceptance: (1) `tldr structure /tmp/tldr-real/dub --format json` reports `language: \"typescript\"` (currently reports `\"python\"`). (2) `tldr impact getHighestSeverity /tmp/tldr-real/dub` picks TypeScript as the language automatically. (3) `grep -rn 'fn from_directory\\|fn detect_lang_from_directory\\|fn detect_language' crates/tldr-core/src crates/tldr-cli/src --include='*.rs'` shows ONE directory detector (`Language::from_directory`) plus legitimate file-path/error-text wrappers, no duplicates in quality/ modules. (4) `cargo test --workspace` passes. (5) `cargo clippy --workspace -- -D warnings` passes.",
      "milestone": "detector-consolidation",
      "status": "passed",
      "depends": ["VAL-001"],
      "worker": "kraken (VAL-002)",
      "evidence": "reports/m2-detector-consolidation.json"
    },
    {
      "id": "VAL-003",
      "type": "behavioral",
      "text": "Re-run the issue's six bug repros on `/tmp/tldr-real/dub` AFTER M1+M2 are complete. Produce a report at `continuum/autonomous/issue-1-bug-fixes/reports/post-consolidation-assessment.md` that, for each of the six bugs in the issue, states: (a) reproduces / does not reproduce, (b) if reproduces, the new (post-consolidation) root cause diagnosis. Expected outcomes based on static analysis: #3 vuln (walker fixed + --lang gating added in M1) → should pass; #4 smells (walker fixed) → should pass; #5 secure (walker fixed) → should pass; #7 structure autodetect (detector fixed in M2) → should pass. Expected to REMAIN broken: #2 change-impact empty-success (single code path swallows three distinct failure modes — `change_impact.rs:231` returns empty OK regardless of whether git-missing or no-changes or user-asked-session-mode), #6 impact 0-callers-with-AST-hit (on dub specifically this is workspace-awareness in the call graph: pnpm monorepo with per-package tsconfigs, `build_project_call_graph(root, lang, None, true)` at `impact.rs:83` doesn't discover sibling workspaces). This assertion's purpose is to confirm which remain and produce a fresh, clean root-cause diagnosis for #2 and #6 uncontaminated by walker/detector noise. The report gates follow-up work — do NOT attempt to fix #2 or #6 in this session; the user wants to assess first.",
      "milestone": "reassess-structural-bugs",
      "status": "passed",
      "depends": ["VAL-001", "VAL-002"],
      "worker": "sleuth (VAL-003)",
      "evidence": "reports/post-consolidation-assessment.md, reports/m3-reassessment.json"
    },
    {
      "id": "VAL-004",
      "type": "invariant",
      "text": "ParserPool selects the correct tree-sitter grammar dialect for `.tsx` and `.jsx` files. Currently `crates/tldr-core/src/ast/parser.rs:40-42` maps `TldrLanguage::TypeScript | TldrLanguage::JavaScript` to `tree_sitter_typescript::LANGUAGE_TYPESCRIPT` regardless of dialect. JSX content causes error-recovery, producing pathological AST that makes `find_message_chains` in `crates/tldr-core/src/quality/smells.rs:1436-1526` run in exponential time (3x per 5 extra lines on dub's `screenshot.tsx`). Fix: make `get_ts_language` and/or `parse_file` path-aware. Preferred shape: add `ParserPool::get_ts_language_for_path(lang: TldrLanguage, path: Option<&Path>) -> Option<Language>` (or rename/replace the existing `get_ts_language`), returning `LANGUAGE_TSX` for `.tsx` and `LANGUAGE_JSX` (if the grammar ships one; tree-sitter-typescript ships `LANGUAGE_TSX` only — verify and use that for both `.tsx` and `.jsx`). `ParserPool::parse_file` at line 124 already has the path — thread it through. For the path-less `ParserPool::parse(source, lang)` API, add a `parse_with_dialect(source, lang, dialect: TsxDialect)` or similar so callers who have path knowledge (callgraph/languages/mod.rs:543 already does this manually) can consolidate. The parser cache keyed by `TldrLanguage` must now key by `(TldrLanguage, Dialect)` or similar. DO NOT split `TldrLanguage::TypeScript` into `TypeScript|Tsx` variants — that would touch hundreds of match sites across the codebase; dialect selection stays inside the parser module. Acceptance: (1) `tldr smells <1584-line-tsx-file>` completes in under 5 seconds (currently times out). (2) `tldr smells /tmp/tldr-real/dub/apps/web --lang typescript --format json --quiet` completes in under 60s (currently times out). (3) A new unit test in parser.rs parses a small .tsx file with JSX (`const X = () => <div>{foo}</div>;`) and asserts zero ERROR nodes in the tree. (4) A new unit test covers message-chain smells on a representative JSX snippet with known chain length — asserts accurate count. (5) The call graph subsystem's existing TSX logic at `callgraph/languages/mod.rs:543` and `callgraph/types.rs:644` CAN migrate to the new consolidated API but this is not required; if left alone, document that both paths produce equivalent results. (6) `cargo test --workspace` no regression. (7) `cargo clippy --workspace -- -D warnings` clean.",
      "milestone": "parser-dialect",
      "status": "pending",
      "depends": [],
      "worker": null,
      "evidence": null
    },
    {
      "id": "VAL-005",
      "type": "invariant",
      "text": "`change_impact_extended` distinguishes 'no changes detected' from 'detection failed' from 'session mode' in its return shape, and the CLI layer maps failure states to non-zero exit codes. Root cause: `crates/tldr-core/src/analysis/change_impact.rs:188-216` collapses `Err(git not available)`, `Err(staged detection failed)`, `Err(uncommitted detection failed)` all into `(vec![], DetectionMethod::Session)` — then `231-244` returns empty OK. Users running `tldr change-impact .` on a clean tree OR without git get identical JSON to a successful analysis of an empty diff. Fix: (a) Add `pub status: ChangeImpactStatus` field to `ChangeImpactReport` struct in the same file (around line 30-47). Enum variants: `Completed`, `NoChanges`, `NoBaseline { reason: String }`, `DetectionFailed { reason: String }`. Use `#[serde(tag = \"kind\", content = \"details\")]` or similar so JSON is readable. (b) In the detection-method dispatch at lines 188-216, replace each silent-downgrade `Err(_) => (vec![], Session)` with propagation: either return early with `NoBaseline { reason }` OR bubble the error up — do NOT silently downgrade. A `GitHead` detection that found zero changes returns `NoChanges` (not `Session`). (c) The CLI wrapper `crates/tldr-cli/src/commands/change_impact.rs:109-158` inspects `report.status` and: `Completed | NoChanges` → exit 0; `NoBaseline | DetectionFailed` → print stderr line `ERROR: change-impact: <reason>; try --files <path> or --base <ref>` and exit code 3. JSON output includes the new `status` field. (d) Update/add tests: clean git tree (HEAD only, no diff) → status `NoChanges`, exit 0. Non-git dir → status `NoBaseline { reason: \"git not available\" }`, exit 3. Explicit `--files` path → status `Completed`. Staged files change → status `Completed`. Acceptance: (1) `tldr change-impact /tmp/tldr-real/dub` on the clean dub fixture (no uncommitted changes) exits 0 with JSON containing `\"status\": {\"kind\": \"NoChanges\"}` (NOT an all-empty generic success). (2) `cd /tmp && mkdir notgit && tldr change-impact /tmp/notgit` (no .git dir) exits 3 with stderr message containing 'baseline' or 'git'. (3) `cargo test --workspace` passes. (4) `cargo clippy --workspace -- -D warnings` clean. Backward-compat: consumers who parse `changed_files`, `affected_tests`, etc. see the same fields; the new `status` field is additive.",
      "milestone": "change-impact-status",
      "status": "pending",
      "depends": [],
      "worker": null,
      "evidence": null
    },
    {
      "id": "VAL-006",
      "type": "invariant",
      "text": "`tldr vuln` has no silent 1000-file truncation cap, and when `--lang` is omitted, it autodetects the project language via M2's `Language::from_directory`. Root cause A: `crates/tldr-cli/src/commands/remaining/vuln.rs:499` — `MAX_DIRECTORY_FILES` predates the walker and is now a vestigial foot-gun that silently truncates dub (3928 TS files) to 1000. After M1's walker hardening, the cap no longer serves a safety purpose; it causes data loss. Root cause B: `vuln.rs:523-549` — with `lang == None`, `is_supported_source_file` falls back to `\"py\" | \"rs\"` only. `tldr vuln .` on a TS repo scans 0 files because no `.py` or `.rs` file is present (outside vendored python tooling), and the user didn't pass `--lang`. Fix A: delete `MAX_DIRECTORY_FILES` and its guard at line 499. If a loud warning is desired at very high file counts, emit stderr (`WARNING: scanned N files, analysis may be slow`) but never drop input. Fix B: when `VulnArgs.lang == None`, call `Language::from_directory(&self.path)` (the M2 detector) in the command's run() method. If detection returns a language in {Python, Rust} (the taint engine's actually-supported set today), use it. If detection returns anything else, emit stderr (`ERROR: vuln does not yet support <language> taint analysis; run with --lang python or --lang rust to scan supported files`) and exit with a diagnostic code (1 or 3). If detection returns None (empty dir), preserve current behavior: empty report, exit 0. Acceptance: (1) `tldr vuln /tmp/tldr-real/dub --lang typescript --format json --quiet` reports `files_scanned >= 3000` (currently 1000 due to cap). (2) `tldr vuln /tmp/tldr-real/dub --format json --quiet` (no --lang) exits non-zero with stderr informing user TS isn't supported and suggesting `--lang python/rust`. (3) `tldr vuln <rust-repo>` (no --lang, e.g. on tldr-code itself) autodetects Rust and scans .rs files. (4) `tldr vuln <python-repo>` autodetects Python and scans .py files. (5) New unit/integration tests in `crates/tldr-cli/tests/` covering all three lang-none branches (supported-by-taint, unsupported-by-taint, empty-dir). (6) `cargo test --workspace` passes, clippy clean.",
      "milestone": "vuln-autodetect-and-cap",
      "status": "pending",
      "depends": ["VAL-002"],
      "worker": null,
      "evidence": null
    },
    {
      "id": "VAL-007",
      "type": "invariant",
      "text": "Call graph construction auto-discovers workspace roots from filesystem markers when no explicit `WorkspaceConfig` is passed; `impact_analysis_with_ast_fallback` emits an accurate note when its scope is limited. Root cause: `crates/tldr-core/src/callgraph/builder.rs:29-51` accepts `Option<&WorkspaceConfig>` but all 18 production callers pass `None` (verified this session: impact.rs:83, hubs.rs:111, dead.rs:123, daemon.rs:366/730/750/780/834, bugbot/first_run.rs:347, archived/arch.rs:132/340, tldr-mcp/src/tools/callgraph.rs:34, tldr-core/src/analysis/whatbreaks.rs, context/builder.rs). The `use_workspace_config: true` branch in `scanner.rs:175-228` and `builder_v2` is dead code in practice. Additionally `module_index.rs:1025-1059` (`detect_ts_base_url`, `detect_ts_paths`) scan only `<root>/tsconfig.json`, so pnpm monorepos with per-app tsconfigs get empty alias tables. Fix: (a) Add `impl WorkspaceConfig { pub fn discover(root: &Path) -> Option<WorkspaceConfig> }` in `crates/tldr-core/src/types.rs` (near the existing struct around line 920). Discovery checks, in order: `pnpm-workspace.yaml` (yaml with `packages:` glob list), root `package.json` with `\"workspaces\": [...]` (npm/yarn/pnpm style), root `Cargo.toml` with `[workspace] members = [...]`, `go.work` with `use` directives. Expand globs via `glob` crate (check if already a dep; if not, use `ignore` crate's overrides). Returns `Some(WorkspaceConfig { roots: [...] })` with absolute paths; `None` if no marker found (preserves current single-root behavior). (b) In `build_project_call_graph` at `builder.rs:29-51`, when `workspace_config` is `None`, call `WorkspaceConfig::discover(root)` and use the result if `Some`. (c) In `module_index.rs`, `detect_ts_paths` and `detect_ts_base_url` become multi-root when `config.use_workspace_config` is true: scan each workspace root's own tsconfig.json, merge alias tables with per-root scoping (e.g. alias `@/` from apps/web/tsconfig.json resolves only within apps/web/**). For simplicity v1 can merge all tsconfig path maps into a single table; improve scoping in a later pass if needed. (d) In `impact_analysis_with_ast_fallback` at `crates/tldr-core/src/analysis/impact.rs:145-164`, when returning a `CallerTree` with `caller_count: 0`, check whether the target function has `export`-visible declarations AND whether `WorkspaceConfig::discover` returned `None` or multiple roots. Refine the `note` text accordingly: (i) isolated private function → keep current `'Function found via AST but has no call edges in analyzed scope'`; (ii) exported function in a workspace monorepo → `'Function is exported but no callers found in workspace roots [/path/a, /path/b, ...]; if this is unexpected, ensure tsconfig.json path aliases resolve correctly or run from a specific package directory'`; (iii) exported function with no discovered workspace → `'Function is exported but no callers found in <root>; if this is a monorepo, the call graph currently scans only the given root — run from the workspace root or a specific package directory'`. Acceptance: (1) `tldr impact getHighestSeverity /tmp/tldr-real/dub --lang typescript` either (a) reports `caller_count >= 1` with the real caller in `apps/web/lib/api/fraud/get-partner-application-risks.ts`, OR (b) returns 0 callers but with the NEW honest note mentioning workspace/exports. Ideally (a). (2) `tldr whatbreaks getHighestSeverity /tmp/tldr-real/dub --lang typescript` same. (3) New unit tests: `WorkspaceConfig::discover` on a tempdir with `pnpm-workspace.yaml` returns workspace members; on `Cargo.toml` with `[workspace]` returns members; on bare dir returns None. (4) New integration test: a small synthetic pnpm-style workspace (apps/foo/tsconfig.json with `\"@/*\":[\"src/*\"]`) where `apps/foo/src/util.ts` exports `myUtil` and `apps/foo/src/app.ts` imports `@/util` and calls `myUtil()` — `tldr impact myUtil` from the workspace root reports >= 1 caller (currently 0). (5) `cargo test --workspace` passes, clippy clean. (6) Existing tests continue to pass — workspace discovery must not change behavior for single-root projects (no workspace markers present).",
      "milestone": "workspace-discovery",
      "status": "pending",
      "depends": [],
      "worker": null,
      "evidence": null
    }
  ]
}
